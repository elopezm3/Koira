<%= simple_form_for @category do |f| %>
  <%= f.input :name, required: true %>
  <%= f.input :photo, as: :file, required: true %>
  <%= f.button :submit, required: true, class:"btn btn-primary" %>
<% end %>
